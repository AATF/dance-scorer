<% @title = "Admin" %>
<% @header = "Admin" %>

<table class="table">
  <tr>
    <th scope="col">Key</th>
    <th scope="col">Value</th>
  </tr>
  <% @settings.each_key do |key| %>
  <tr>
    <td><%= key %></td>
    <td><%= link_to 'edit', edit_admin_setting_path(key) %></td>
  </tr>
  <% end %>
</table>
