<% @title = "users" %>

<h1>Judges index</h1>

go to <%= link_to "home", root_path %>

<table>
  <tr>
    <th>Username</th>
    <th>Name</th>
  </tr>
  <% @users.each do |user| %>
    <tr>
      <td><%= user.username %></td>
      <td><%= user.name %></td>
    </tr>
  <% end %>
</table>
